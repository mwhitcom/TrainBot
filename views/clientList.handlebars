<h1>Client List</h1>
{{#each clients}}
    <div>
        <h1>Name: {{name}}</h1>
        <h3>Email: {{email}}</h3>
        <h3>User Name: {{username}}</h3>
        
        <p>Program: {{Program.name}}</p> 
    </div>
    <br>
{{/each}}